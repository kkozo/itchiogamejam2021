git diff# Gamejam2021

## Development server

Run `npm run-script dev` for a dev server. Navigate to `http://localhost:8080/`. The app will automatically reload if you change any of the source files.

